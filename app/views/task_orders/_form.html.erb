<%= form_for(@task_order) do |f| %>
  <% if @task_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task_order.errors.count, "error") %> prohibited this task_order from being saved:</h2>

      <ul>
      <% @task_order.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :game_id %><br />
    <%= f.select :game_id, options_from_collection_for_select(Game.all, :id, :name) %>
  </div>
  <div class="field">
    <%= f.label :team_id %><br />
    <%= f.select :team_id, options_from_collection_for_select(Team.all, :id, :name) %>
  </div>
  <div class="field">
    <%= f.label :task_id %><br />
    <%= f.select :task_id, options_from_collection_for_select(Task.all, :id, :name) %>
  </div>
  <div class="field">
    <%= f.label :order_n %><br />
    <%= f.number_field :order_n, :in => 1...10 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
